#include <stdio.h>
#include <stdint.h>
#include<math.h>
#define MAX 100
typedef long long ll;

int main() {

    	int m, n;
    	scanf("%d %d", &m, &n);
    	int a = m + n;
    	printf("%d", a);

	return 0;
}

